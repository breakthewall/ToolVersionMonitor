from os import (
    path as os_path
)

HERE = os_path.dirname(os_path.abspath( __file__ ))
BADGES_PATH = os_path.join(HERE, 'badges')
STATIC_PATH = os_path.join(HERE, 'static')
DATA_PATH = os_path.join(HERE, 'data')